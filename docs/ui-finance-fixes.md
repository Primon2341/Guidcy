# Guidcy UI and earnings fixes

Changes are local to this workspace; no deployment, production mutation, or live payment charge was performed.

## Root causes and changes

| Issue | Root cause | Implementation / files |
| --- | --- | --- |
| Payment Back to profile | The button relied on browser history rather than the locked booking's consultant. The profile adapter also pushed duplicate history entries when the router was already at the correct URL, which broke Forward. | `assets/js/app.js`: `guidcyPaymentConsultantId`, `guidcyPaymentBack`, and `openProfile` adapter use the existing booking consultant ID and SPA navigation, and avoid duplicate profile history entries. Busy/payment locks remain in place. |
| Inconsistent rupee glyph | The self-hosted Latin font subsets excluded U+20B9, leaving the rupee to a different fallback font. Monetary displays also mixed manual prefixes, rounding, and locale formatting. | `assets/css/fonts.css`, four `assets/fonts/*-inr.woff2` files, and font preloads in `index.html` add U+20B9 subsets of the existing font families. `assets/js/app.js` adds the shared `guidcyFormatINR` using `Intl.NumberFormat('en-IN', {currency:'INR', style:'currency'})`; checkout keeps two decimal places. App and webinar currency renderers use it while preserving existing Free/empty-state meanings. Font provenance and licenses are in `assets/fonts/README.md` and the three OFL files. |
| Excessively heavy typography | Shared styles, inline templates, and generated styles repeatedly used 700/800 weights without a consistent hierarchy. | `assets/css/base.css` defines regular 400, medium 500, semibold/title 600, and brand 700 tokens. `assets/css/patches.css`, `assets/css/login.css`, `index.html`, `assets/js/app.js`, and `assets/js/webinar-flow.js` apply these tokens. Existing families and the brand's heavier weight remain. Standalone email HTML remains self-contained. |
| Earnings trend | The chart rendered fixed demo bars rather than booking data. Legacy dashboard wrappers could reject an accepted refresh as a duplicate; repeated restoration could restart slow queries. Failed queries also needed to remain distinguishable from empty results. | `assets/js/app.js`: Overview reuses its consultant-scoped bookings read. `guidcyEarningsMonths` aggregates 12 calendar months in IST using the existing payout eligibility predicate and payout calculation. Completion date is preferred, with session/creation date fallback for older records. Demo markup is replaced by real bars or “No earnings data yet”. Existing booking Realtime invalidates cached reads and refreshes the active tab. Legacy wrappers stop reinstalling after the route controller takes ownership; one in-flight read per dashboard tab prevents repeated reload queries, with one follow-up when data changes mid-read. The existing load-error Retry button now invokes the dashboard controller. Chart presentation is in `assets/css/base.css`. |
| Clipped My bookings title | An absolutely positioned skeleton pseudo-element covered the bottom of the heading during loading. It was an overlay collision, not truncated font glyphs. | `assets/css/base.css` places the skeleton below the title in normal document flow; `assets/css/patches.css` removes conflicting skeleton overrides. `assets/js/app.js` keeps the section title in the initial loading markup. Existing content remains during background revalidation. No artificial delay or heading concealment is used. |
| Remove payment overview card (latest request) | The reference-style checkout included a duplicate total and benefits column. | `index.html` removes the entire `pay-overview` aside. `assets/css/patches.css` centers a compact single column, with a width constraint that also survives older mobile max-width rules. Obsolete overview styles and DOM writes are removed from `assets/css/base.css`, `assets/js/app.js`, and `assets/js/webinar-flow.js`. Session and payment amounts stay in the primary form; webinar checkout includes its total in the primary summary. |

Earnings eligibility remains the existing settlement rule: verified successful payment, completed booking and session, and pending/paid payout status. The stored consultant payout is authoritative, with the existing payout fallback when absent. Neither gross customer payments nor pending/cancelled/unverified bookings are charted as earnings. No schema, RLS, payment creation/verification, fee, promo, meeting, email, or authentication provider changes were made.

## Validation

- Production build and `git diff --check`.
- All 264 Node regression tests, including new `tests/earnings-currency.test.mjs`: INR precision/grouping, eligibility exclusions, consultant isolation, deduplication, stored/fallback payout, IST month boundary, date fallback, and true empty state.
- `tests/dashboard-earnings-loading-browser.test.cjs`: Chrome at 390 and 1280 px; four refreshes including cleared/disabled browser cache; delayed fonts, auth and booking queries; heading/skeleton geometry during loading; tab navigation; Realtime-driven earning creation/update/removal; query-error/retry recovery; actual rendered font inspection for rupee and digits in DM Sans, Cormorant Garamond and Plus Jakarta Sans.
- `tests/repeated-refresh-layout-browser.test.cjs`: four consecutive refreshes across seven public routes and user/consultant/admin dashboards at mobile/desktop widths; stable header, correct URL and footer end. Passed before the final checkout-only card removal.
- `tests/mobile-dashboard-menu.test.cjs`: mobile menu, all role links, Back/Forward and responsive behavior.
- `tests/auth-navigation-browser.test.cjs`: all three roles at 390 and 1280 px; Google callback return routes, password auth, logout/re-login, defaults, passive auth events, Back/Forward and wrong-role rejection.
- Read-only inspection of the connected Supabase schema and existing booking eligibility/monthly payout aggregation. No production rows were modified. Realtime UI behavior was exercised using controlled browser fixtures.
- Browser screenshots inspected for desktop/mobile dashboard headings and final checkout layout.

`tests/payment-checkout-ui-browser.test.cjs` passed on the final single-column layout at 320–1365 px: actual slot selection, dynamic consultant/fee values, Back to profile plus browser Back/Forward after reload and direct payment entry, invalid/valid promo, promo restoration, checkout cancellation/failure, rejected verification, and the successful order → Razorpay → server verification → meeting → confirmation/email sequence. Razorpay and external provider callbacks in browser tests are controlled fixtures; these tests do not claim a live bank/payment-provider transaction.

The existing `tests/browser-flow.test.cjs` also completed successfully on the final build, including payment cold restoration, booking deep links, Back navigation, public/mobile routing, finder hydration, purchased notes and admin route checks. Its old 700-weight expectation was updated to the requested 600 semibold token, and removed overview selectors now inspect the main summary. One prior run stopped at an intermittent expert-card avatar-size assertion; a diagnostic rerun passed the full suite without an application change for that assertion. Test screenshots now go to `/private/tmp` instead of overwriting tracked images.

## Follow-up: dashboard links and visible earnings amounts

The earlier URL-only mobile link check was insufficient: it did not ensure the requested dashboard content actually loaded. The replacement `tests/dashboard-tabs-browser.test.cjs` verifies content, selected navigation item, URL, refresh restoration and runtime errors for every registered tab.

- **Marketplace / Purchased Notes regression:** the legacy Marketplace `wrapRoutes()` ran from DOMContentLoaded and timers after the route controller initialized. It replaced the public dashboard switcher with a renderer that bypassed route ownership; My Marketplace could show its older sales table while the URL remained Overview. Purchased Notes also depended on that late wrapper registration. In `assets/js/app.js`, the legacy adapter now stops installing dashboard wrappers once the controller exists. `invokeDashboard()` explicitly dispatches the current existing Marketplace admin/seller/purchases renderers, passing the correct user or consultant panel ID. Existing purchase reconciliation remains in that call path.
- **Earnings amounts:** `guidcyRenderEarningsTrend()` in `assets/js/app.js` now displays the total earned during the displayed 12-month period plus each month's exact formatted INR amount. `assets/css/base.css` keeps the labels legible; on small screens the chart scrolls internally and opens on the most recent months. Eligibility and payout calculations are unchanged.
- **Admin Featured Experts:** full-tab verification uncovered a parent panel left in its loading state after the renderer updated only a nested container. `buildFeatureAdmin()` now commits its completed controls through the panel setter, and its caller returns the actual rendering promise instead of scheduling an untracked timer. Existing role/epoch guards remain.
- **Mobile drawer during loading:** the slow-font test reproduced the drawer closing on an ordinary late `pageshow` event. `assets/js/controllers.js` now closes on `pageshow` only for a back/forward-cache restoration (`persisted`), preserving a menu the user has opened during a normal load. Navigation, Escape, overlay and explicit close controls retain their existing behavior. `build-static.js` adds content-based cache versioning for this controller asset.

- **Delayed navigation registration:** Consultant Notifications and Admin Support Tickets were installed only after load/timers. A direct Notifications refresh rendered content without a selected sidebar item. Their existing navigation installers now also run synchronously before route initialization in `assets/js/app.js`; the final dashboard test explicitly includes both tabs.

Validation: all 43 registered tabs (11 user, 13 consultant, 19 admin) loaded correct content and restored after refresh at both 1280px and 390px. Marketplace-related tabs additionally passed three consecutive reloads and Back/Forward checks; fixtures included actual owned purchase/listing rows, rather than only empty states. The focused earnings/slow-load test passed at both widths, including visible totals/month values updating from ₹850 to ₹425, no earnings after cancellation, error/retry recovery, normal pageshow preserving the drawer and BFCache pageshow dismissing it. All 265 Node regression tests passed. These browser runs use controlled local Supabase fixtures and perform no production writes.

## Follow-up: Razorpay opening performance and webinar checkout titles

Root causes: two separate checkout SDK loaders, warm-up tied to only some `go('payment')` calls, and an outer opener adding two animation frames plus 90ms on mobile (20ms desktop). Booking and Marketplace called the captured inner opener, so setup/cleanup and warm-up differed between flows. A rejected shared SDK promise also remained cached, preventing a clean retry. Marketplace performed independent note and ownership reads sequentially; consultant contact lookup could repeat identical IDs.

- `assets/js/razorpay-checkout.js`: one shared, retryable SDK loader; preconnect and preload on consultant profile, payment, webinar and Marketplace entry, plus payment intent. It observes actual page activation rather than replacing router functions. Unrelated pages do not download the SDK. Preloading neither creates orders nor opens/charges checkout.
- `assets/js/app.js`: all existing payment flows use that loader and the same immediate opener/cleanup. Removed the delayed wrapper. SDK loading can overlap order preparation; independent Marketplace reads run together, and duplicate consultant lookup IDs are removed. Server order creation, authoritative prices, verification, booking, meeting and email sequencing remain unchanged.
- `assets/js/webinar-flow.js` and `assets/css/patches.css`: the Webinar label can no longer shrink into multiple lines beside a long title. Checkout rows use a fixed-content label column and a flexible value column; the complete title wraps only within its own column without horizontal page overflow.
- `index.html` / `build-static.js`: load, minify and content-version the shared loader before the application.

Validation: production build; 265 Node regressions; existing consultation checkout success/cancellation/failure/rejected-verification browser suite; new `tests/razorpay-opening-browser.test.cjs` at mobile/desktop widths with 700ms simulated SDK delivery and 350ms simulated order response. The warmed opener invoked Razorpay 2–4ms after the order became ready, with no animation-frame/fixed-delay wait. This measures local application overhead, **not live Razorpay iframe rendering or production network/API latency**. Verified webinar payment verification, Marketplace cancellation/cleanup, one shared script, retry after SDK download failure, no speculative order creation/opening, and preloading from all three entry routes. Long titles and single-line field labels were checked at 320, 390, 430 and 1280px. No live charge was made.

The existing server-created order and server verification sequence is retained, consistent with the [Razorpay integration documentation](https://razorpay.com/docs/payments/server-integration/nodejs/integration-steps/).

## Follow-up: public webinar count and card loading

Root cause: the authoritative `wbnLoad` waited for the webinar list, then publisher profile photos, then registration totals before rendering anything. Superseded startup synchronizers and multiple timed hydration passes also issued reads and invoked the renderer repeatedly. Empty successful responses incorrectly triggered a second query.

Changes in `assets/js/app.js`:

- Start the list and registration reads together; render the actual upcoming count and cards immediately when the list arrives. Publisher photos and availability update their own card elements afterward. Registration controls wait for the initial availability result, using the existing count/error semantics.
- Skip superseded local-storage synchronizers once the authoritative loader is installed. Replace startup timer ladders with the existing route/DOM/visibility events and the shared in-flight request.
- Preserve unchanged card elements, expanded descriptions and focus across secondary responses and repeated render calls. Retain the existing list (including a valid empty list) during revalidation and failed refreshes; distinguish a failed query from a successful empty response.
- Invalidate the short-lived webinar read cache when a webinar Realtime change arrives.

Validation: production build and all 265 Node tests passed. `tests/webinar-loading-browser.test.cjs` passed at 390px and 1280px, three consecutive refreshes each. With photo and registration requests explicitly held pending, the correct count/cards appeared first; each refresh made one list REST request. Verified full-seat controls, late photos without card replacement, expanded descriptions, empty results, failed refresh retaining data, subsequent updates and SPA re-entry. Screenshots were captured for both viewports and the mobile result was inspected. `tests/webinar-card.test.mjs` now scopes its click-delegation assertion to the handler, avoiding a false positive from the new card-detail updater. A read-only Supabase aggregate confirmed the webinar source is accessible; no production rows were changed.

Additional focused browser checks passed for user sign-in at 390px and consultant sign-in at 1280px: account gate, return to the selected webinar, free registration, duplicate-registration protection and paid-registration form entry. The broader pre-existing account/dashboard suite did not pass: the changed version encountered a detached Completed-tab button; a separate unchanged-HEAD run also failed, at the signup auto-registration expectation. Those broader failures are not reported as fixed by this public-page loading change. No live payment was made and these changes have not been deployed.
